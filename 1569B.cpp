#include <bits/stdc++.h>
using namespace std;
#define ll long long int
int main()
{
    ll tt;
    cin >> tt;
    while (tt--)
    {
        int n;
        string s;
        cin >> n >> s;
        if (s.size() == 1 || s.size() == 2)
        {
            cout << "NO" << endl;
            continue;
        }
    }
    return 0;
}
